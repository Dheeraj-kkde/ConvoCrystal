/**
 * Re-exports from /src/app/stores/userStore.
 * Kept for backward-compatible imports across the codebase.
 */
export { useUserStore, useUser, ONBOARDING_STEPS } from "../stores/userStore";
export type { OnboardingStep, UserState } from "../stores/userStore";
