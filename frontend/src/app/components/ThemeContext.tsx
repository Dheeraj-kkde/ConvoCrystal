/**
 * Re-exports from /src/app/stores/themeStore.
 * Kept for backward-compatible imports across the codebase.
 */
export { useThemeStore, useTheme } from "../stores/themeStore";
export type { ThemeColors, ThemeState } from "../stores/themeStore";
